package com.piusxi.student.backend;

public class createAccount {

    /**
     * This function will insert the values from the createAccountForm.java into the student_info database
     * createAccount.insert()
     * @param firstName -> obvious
     * @param lastName -> obvious
     * @param studentId -> unique student ID number
     * @param email -> obvious
     * @param password -> obvious
     * 
     * Gonna hash it before adding it maybe (not necessary but i get bored)
     */
    public void insert(String firstName, String lastName, int studentId, String email, String password) {
        
    }
    public static void main(String[] args) {
        /*
         * Insert values into database
         */
    }
}
