package com.piusxi.student.backend;

// import com.piusxi.student.frontend.createAccountForm;

public class createAccount {
    public static void main(String[] args) {

        /*
         * When the 'Create Account' button in createAccountForm.java is clicked
         * Get the values from the text fields
            * First Name
            * Last Name
            * Student ID
            * Email
        * Skip password because that is going to get passed to passwordHash.java and from there be inserted into the database
        * Add them to database with unverified state next to email
        * Send verification email
        * When email is verified, update verified state and allow login 
         */
    }
}
